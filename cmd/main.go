package main

func main() {
	// validate no. of arguments sent to binary
	// and print usage if incorrect

	// create db client

	// create queue client

	// spawn async worker that keeps reading from queue and processes
	// messages i.e inserts to database.

	// wait for signal to exit the process

	// after getting signal, ensure async goroutines exit cleanly
}
